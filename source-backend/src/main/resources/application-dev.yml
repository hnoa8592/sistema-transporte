spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sistema_transporte
    username: postgres
    password: postgres

  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    com.transporte: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    org.springframework.security: DEBUG
